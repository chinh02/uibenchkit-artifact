# Required only for the real-model smoke test and generation runs.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# Local service authentication. Compose binds both services to loopback.
UIBENCHKIT_API_KEY=artifact-local-key

# Optional for private repositories or higher GitHub API rate limits.
GITHUB_TOKEN=
GITHUB_REPO=chinh02/uibenchkit-experiments
GITHUB_BRANCH=main

